# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository (`barkingpuppy`) initialized with only a `README.md`.
There is currently no application code, no dependencies, and no services to run.

- **Tech stack:** Not yet determined.
- **Build/run/test commands:** None defined yet. When code is added, update this section with the relevant commands.
- **Available system tools:** Node.js v22, npm v10, Python 3.12 are pre-installed in the cloud VM.

When a tech stack and application code are added to this repo, update this file with:
1. How to install dependencies (and the update script via `SetupVmEnvironment`).
2. How to run, build, lint, and test the application.
3. Any non-obvious caveats discovered during setup.
